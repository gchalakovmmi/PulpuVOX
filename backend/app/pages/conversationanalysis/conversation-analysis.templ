package conversationanalysis

import (
				"PulpuVOX/pages/base"
				"PulpuVOX/pages/conversationanalysis/components/mock"
				"github.com/markbates/goth"
)

templ ConversationAnalysisStyles() {
				@Styles()
				@mock.Styles()
}

templ ConversationAnalysisScripts() {
				@Scripts()
				@mock.Scripts()
}

templ ConversationAnalysisComponents(user *goth.User) {
				@mock.Mock(user)
}

templ ConversationAnalysis(user *goth.User) {
				@base.Base("PulpuVOX", ConversationAnalysisComponents(user), ConversationAnalysisStyles(), ConversationAnalysisScripts(), user)
}
